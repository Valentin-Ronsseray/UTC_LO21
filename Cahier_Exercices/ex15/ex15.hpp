#ifndef EX15
#define EX15

#include <iostream>
#include <string>
#include <limits>

using namespace std;

unsigned int minimum(unsigned int a = numeric_limits<unsigned int>::max(), unsigned int b = numeric_limits<unsigned int>::max(), unsigned int c = numeric_limits<unsigned int>::max(), unsigned int d = numeric_limits<unsigned int>::max(), unsigned int e = numeric_limits<unsigned int>::max());

#endif // EX15